void print_max(int* pa) ; //함수선언

int main()

{
    int array[7] = { 4, 5, 8, 1, 2, 3, 7};

    print_max(array);
    return 0;
}

void print_max(int* pa)
{
    int max=0;
    for(int i=0; i<7; i++){
        if(max<pa[i])
            max = pa[i]
    }
    printf("%d", max)
}
