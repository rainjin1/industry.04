#include <stdio.h>
int plus(int a, int b){
    return a+b ;
}
int minus(int a, int b){
    return a-b;
}

int main()
{
    int (*fp)(int, int); //이게 포인터를 변수화 시킨것
    int res;

    fp = plus;
    res = fp(10, 20);
    printf("%d\n", res);
    return 0;
}
