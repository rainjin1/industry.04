#include <stdio.h>

int main()
{
    int arr[2][2]={{1,2},{3,4}}; //선언과 동시 초기화 가능
    for (int i = 0; i < 2; i++)
    {
        for (int j = 0; j < 2; j++)
        {
            arr[i][j];
            printf("%3d, %d", i, j);
        }
        printf("\n");
    }        
    return 0;
}
