#define _CRT_SECURE_NO_WARNINGS 
#include <stdio.h>

int sum(int x, int y)
{
	int temp = x + y;
	return temp;
}
int main()
{
	int a, b;
	scanf("%d", &a);
	scanf("%d", &b);
	int result = sum(a, b);

	printf("result:%d", result);

	return 0;
}
