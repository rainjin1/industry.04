#define _CRT_SECURE_NO_WARNINGS 
#include <stdio.h>
int main()
{
	int i, dan;
	for (i = 9, dan=5; i >0; i--)
	{
		printf("%d * %d = %d\n", dan, i, dan * i);
	}
	printf("\n\n");
	dan = 3, i=9;
	while(i>=1)
	{
		printf("%d * %d = %d\n", dan, i, dan * i);
		i--;
	}
	return 0;
}
