#define _CRT_SECURE_NO_WARNINGS 
#include <stdio.h>
int main()
{
	for (int dan = 2; dan < 10; dan++)
	{
		for (int i = 1; i <= 9; i++)
		{
			printf("%d * %d=%d\n", dan, i, dan * i);
		}
	}
	return 0;
}
