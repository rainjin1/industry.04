#define _CRT_SECURE_NO_WARNINGS 
#include <stdio.h>
int main()
{
	int score[3]; //성적, 순서는 국 영 수
	int total=0; //총점
	double avg = 0; //평균
	for (int i = 0; i < 3; i++)
	{
		scanf("%d", &score[i]);
		total += score[i];
	}
	avg = total / 3.0;
	printf("%d %.2lf", total, avg);

	return 0;
}
