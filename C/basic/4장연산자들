#define _CRT_SECURE_NO_WARNINGS 
#include <stdio.h>
int main()
{
	int a = 10, b = 20;
	int res;
	res = (a > b);
	printf("%d\n", res);
	res = (b > a);
	printf("%d\n", res);
	//참이면 1을 표시, 거짓이면 0을 표시.
	// 나누기 /, 그런데 몫만 나옴. %를 쓰면 나머지만나옴
	//느낌표 ! 를 붙이면 not 이라는 뜻. &&=and \\=or
	res = !res;
	printf("%d", res);

	//sizeof = 변수의 크기를 알려줌
	int c = 10;
	double d = 3.4;

	printf("int의 변수의 크기: %d\n", sizeof(c));
	printf("double의 변수의 크기: %d\n", sizeof(d));
	printf("정수형 변수의 크기: %d\n", sizeof(10));
	printf("수식의 결괏값의 크기: %d\n", sizeof(1.5 + 3.4));
	printf("char 자료형의 크기: %d\n", sizeof(char));

	//복합대입연산자 +=, *= 계산해서 대입한다. ex) a += 10 => a = a+10
	//콤마 연산자. 하나씩 진행하고, 마지막값을 저장함.

	int A = 10, B = 20, result;
	//if (A>B)
	//{
	//	result = A;
	//}
	//else
	//{
	//	result = B;
	//}

	result = (A > B) ? a : b;
	printf("큰값: %d", result);

	/*
	&는 둘다 1이면 1
	^ exclusive or 같으면 0, 다르면 1. 둘다 1이면 0됨.
	\ or 둘중하나라도 1이면 1
	~ 보수값.
	<< n = 왼쪽으로 n비트 이동. 그러니까 0110 을 << 1 하면, 1100이됨. *2한 효과가 됨.
	>> n = 오른쪽으로 n비트이동. 1비트당 /2함. 몫만 결과로 남음.
	연산자 우선순위 참고
	*/
	
	return 0;
}
